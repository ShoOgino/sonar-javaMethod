  void foo2() {
    if ("red".equals(choice)) {  // compliant, not enough choices
      dispenseRed();
    } else if ("blue".equals(choice)) {
      dispenseBlue();
    } else {
      promptUser();
    }
  }

