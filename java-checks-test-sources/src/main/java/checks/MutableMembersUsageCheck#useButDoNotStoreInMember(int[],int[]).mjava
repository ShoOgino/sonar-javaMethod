  public int[] useButDoNotStoreInMember(int[] arr1, int[] arr2) {
    int[] tmp;
    if (arr1.length >= arr2.length) {
      tmp = arr1; // Compliant
    } else {
      tmp = arr2; // Compliant
    }
    return tmp;
  }

