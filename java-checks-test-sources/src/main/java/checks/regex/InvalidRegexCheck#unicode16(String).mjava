  void unicode16(String str) {
    str.matches("[😂😊]"); // Compliant
    str.matches("[^\ud800\udc00-\udbff\udfff]"); // Compliant
    str.matches("[^\\ud800\\udc00-\\udbff\\udfff]"); // Compliant
  }

