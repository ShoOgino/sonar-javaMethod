  @Test
  void testComplex3() {
    setup("Always the same, no need to parameterize");
    Object o = getObject(333);
    assertNotNull(o);
    String s = o.toString();
    assertEquals(s.length(), 3);
  }

