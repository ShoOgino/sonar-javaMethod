  @Test
  void testComplex2() {
    setup("Always the same, no need to parameterize");
    Object o = getObject(22);
    assertNotNull(o);
    String s = o.toString();
    assertEquals(s.length(), 2);
  }

