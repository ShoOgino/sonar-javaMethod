    private static boolean count(java.util.List<String> words) {
      boolean result = false;
      for (String word : words) {
        if (isWord(word)) {
          result = true;
        }
      }
      return result;
    }

