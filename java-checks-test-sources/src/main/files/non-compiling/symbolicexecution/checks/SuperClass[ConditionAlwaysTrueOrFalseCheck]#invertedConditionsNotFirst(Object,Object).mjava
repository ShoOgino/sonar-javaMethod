  public void invertedConditionsNotFirst(Object a, Object b) {
    if (a != b) {
      if ( a == b) {   // Noncompliant
        log("Are same!");
      } else {
        log("Not same!");
      }
    }
  }

