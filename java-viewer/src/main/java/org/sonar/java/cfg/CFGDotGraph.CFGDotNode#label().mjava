    @Override
    public String label() {
      String label = "B" + blockId;
      if (isExitBlock) {
        label += " (EXIT)";
      } else if (isFirstBlock) {
        label += " (START)";
      }
      return label;
    }

