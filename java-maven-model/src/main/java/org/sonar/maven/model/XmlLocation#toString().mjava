  @Override
  public String toString() {
    String columnAsText = Integer.toString(column);
    if (column == -1) {
      columnAsText = "?";
    }
    return "(" + line + "," + columnAsText + ")[" + offset + "]";
  }

