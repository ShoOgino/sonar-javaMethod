  public LCS(String inputI, String inputJ) {
    checkNotNull(inputI);
    checkNotNull(inputJ);

    this.inputI = inputI;
    this.inputJ = inputJ;
    this.c = new int[inputI.length() + 1][inputJ.length() + 1];
  }

