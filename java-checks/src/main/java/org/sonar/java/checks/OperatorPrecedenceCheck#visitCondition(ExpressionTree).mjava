  private void visitCondition(@Nullable ExpressionTree condition) {
    if (condition != null) {
      inCondition = true;
      scan(condition);
      inCondition = false;
      if (hasIssue) {
        raiseIssue(condition);
      }
    }
  }

