  public List<T> getExtraction(List<Integer> indexes) {
    List<T> results = Lists.newArrayList();

    for (Integer i : indexes) {
      results.add(input.elementAt(i));
    }

    return results;
  }

