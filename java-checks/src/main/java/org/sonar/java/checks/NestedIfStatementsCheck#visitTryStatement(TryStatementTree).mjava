  @Override
  public void visitTryStatement(TryStatementTree tree) {
    nestingLevel++;
    checkNesting(tree);
    scan(tree.block());
    nestingLevel--;
    scan(tree.resources());
    scan(tree.catches());
    scan(tree.finallyBlock());
  }

