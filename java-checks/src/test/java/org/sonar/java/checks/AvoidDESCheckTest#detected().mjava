  @Test
  public void detected() {
    SourceFile file = JavaAstScanner.scanSingleFile(new File("src/test/files/checks/AvoidDESCheck.java"), new VisitorsBridge(new AvoidDESCheck()));
    checkMessagesVerifier.verify(file.getCheckMessages())
        .next().atLine(6).withMessage("Use the recommended AES (Advanced Encryption Standard) instead.")
        .next().atLine(7).withMessage("Use the recommended AES (Advanced Encryption Standard) instead.")
    ;
  }

