  @Test
  public void detected() {
    SourceFile file = JavaAstScanner.scanSingleFile(new File("src/test/files/checks/ImplementsEnumerationCheck.java"), new ImplementsEnumerationCheck());
    checkMessagesVerifier.verify(file.getCheckMessages())
        .next().atLine(2).withMessage("Implement Iterator rather than Enumeration.")
        .next().atLine(12)
        .next().atLine(18);
  }

