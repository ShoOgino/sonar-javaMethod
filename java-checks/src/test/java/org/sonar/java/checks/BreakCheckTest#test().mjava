  @Test
  public void test() {
    SourceFile file = JavaAstScanner.scanSingleFile(new File("src/test/files/checks/Break.java"), check);
    CheckMessagesVerifier.verify(file.getCheckMessages())
        .next().atLine(10).withMessage("The 'break' branching statement prevents refactoring the source code to reduce the complexity.")
        .noMore();
  }

