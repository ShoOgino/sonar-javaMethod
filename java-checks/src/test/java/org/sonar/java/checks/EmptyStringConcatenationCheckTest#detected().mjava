  @Test
  public void detected() {
    SourceFile file = JavaAstScanner.scanSingleFile(new File("src/test/files/checks/EmptyStringConcatenationCheck.java"), new EmptyStringConcatenationCheck());
    checkMessagesVerifier.verify(file.getCheckMessages())
        .next().atLine(3).withMessage("Replace this empty String concatenation by a method such as String.valueOf() or Object.toString().")
        .next().atLine(4)
        .next().atLine(7);
  }

