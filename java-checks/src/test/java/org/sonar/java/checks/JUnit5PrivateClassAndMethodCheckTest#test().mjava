  @Test
  void test() {
    JavaCheckVerifier.newVerifier()
      .onFile(testSourcePath)
      .withCheck(new JUnit5PrivateClassAndMethodCheck())
      .verifyIssues();

    JavaCheckVerifier.newVerifier()
      .onFile(testSourcePath)
      .withCheck(new JUnit5PrivateClassAndMethodCheck())
      .withoutSemantic()
      .verifyNoIssues();
  }

