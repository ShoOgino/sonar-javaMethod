  @Test
  public void test() {
    SourceFile file = JavaAstScanner.scanSingleFile(new File("src/test/files/checks/HasNextCallingNextCheck.java"),
      new VisitorsBridge(new HasNextCallingNextCheck()));
    checkMessagesVerifier.verify(file.getCheckMessages())
      .next().atLine(9).withMessage("Refactor the implementation of this \"Iterator.hasNext()\" method to not call \"Iterator.next()\".")
      .next().atLine(10)
      .next().atLine(52);
  }

