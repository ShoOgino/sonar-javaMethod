  @Test
  public void detected() {
    SourceFile file = JavaAstScanner.scanSingleFile(new File("src/test/files/checks/ReplaceLambdaByMethodRefCheck.java"), new VisitorsBridge(new ReplaceLambdaByMethodRefCheck()));
    checkMessagesVerifier.verify(file.getCheckMessages())
      .next().atLine(6).withMessage("Replace this lambda with a method reference.")
      .next().atLine(7).withMessage("Replace this lambda with a method reference.")
      .next().atLine(13).withMessage("Replace this lambda with a method reference.")
      .next().atLine(15).withMessage("Replace this lambda with a method reference.")
    ;
  }

