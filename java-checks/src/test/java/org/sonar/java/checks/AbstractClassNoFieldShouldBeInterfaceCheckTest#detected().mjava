  @Test
  public void detected() {
    SourceFile file = JavaAstScanner.scanSingleFile(new File("src/test/files/checks/AbstractClassNoFieldShouldBeInterfaceCheck.java"), new VisitorsBridge(new AbstractClassNoFieldShouldBeInterfaceCheck()));
    checkMessagesVerifier.verify(file.getCheckMessages())
        .next().atLine(6).withMessage("Convert the abstract class \"B\" into an interface");
  }

