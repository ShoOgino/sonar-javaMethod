  @Test
  public void detected() {
    SourceFile file = JavaAstScanner.scanSingleFile(new File("src/test/files/checks/CatchOfThrowableOrErrorCheck.java"), new VisitorsBridge(new CatchOfThrowableOrErrorCheck()));
    checkMessagesVerifier.verify(file.getCheckMessages())
      .next().atLine(5).withMessage("Catch Exception instead of Throwable.")
      .next().atLine(6).withMessage("Catch Exception instead of Throwable.")
      .next().atLine(7).withMessage("Catch Exception instead of Error.")
      .next().atLine(10).withMessage("Catch Exception instead of Error.")
      .next().atLine(14)
      .next().atLine(16).withMessage("Catch Exception instead of Throwable.")
      .next().atLine(17).withMessage("Catch Exception instead of Error.");
  }

