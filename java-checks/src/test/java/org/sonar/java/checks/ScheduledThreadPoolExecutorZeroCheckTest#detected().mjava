  @Test
  public void detected() {
    SourceFile file = JavaAstScanner.scanSingleFile(new File("src/test/files/checks/ScheduledThreadPoolExecutorZeroCheck.java"), new VisitorsBridge(
      new ScheduledThreadPoolExecutorZeroCheck()));
    checkMessagesVerifier.verify(file.getCheckMessages())
      .next().atLine(5).withMessage("Increase the \"corePoolSize\".")
      .next().atLine(7).withMessage("Increase the \"corePoolSize\".")
      .noMore();
  }

