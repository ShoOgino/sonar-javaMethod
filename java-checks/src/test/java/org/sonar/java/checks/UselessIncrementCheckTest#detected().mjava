  @Test
  public void detected() {
    SourceFile file = JavaAstScanner.scanSingleFile(new File("src/test/files/checks/UselessIncrementCheck.java"), new VisitorsBridge(new UselessIncrementCheck()));
    checkMessagesVerifier.verify(file.getCheckMessages())
        .next().atLine(8).withMessage("Remove this increment or correct the code not to waste it.")
        .next().atLine(9).withMessage("Remove this increment or correct the code not to waste it.")
        .next().atLine(10).withMessage("Remove this increment or correct the code not to waste it.")
      ;
  }

