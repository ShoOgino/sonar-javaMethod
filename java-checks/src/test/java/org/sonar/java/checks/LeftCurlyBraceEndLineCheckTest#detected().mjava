  @Test
  public void detected() {
    SourceFile file = JavaAstScanner.scanSingleFile(new File("src/test/files/checks/LeftCurlyBraceEndLineCheck.java"), new LeftCurlyBraceEndLineCheck());
    checkMessagesVerifier.verify(file.getCheckMessages())
        .next().atLine(4).withMessage("Move this left curly brace to the end of previous line of code.")
        .next().atLine(11);
  }

