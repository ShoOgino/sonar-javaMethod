  @Test
  public void detected() {
    SourceFile file = JavaAstScanner.scanSingleFile(new File("src/test/files/checks/WaitInWhileLoopCheck.java"), new VisitorsBridge(new WaitInWhileLoopCheck()));
    checkMessagesVerifier.verify(file.getCheckMessages())
        .next().atLine(10).withMessage("Remove this call to \"wait\" or move it into a \"while\" loop.")
        .next().atLine(11).withMessage("Remove this call to \"await\" or move it into a \"while\" loop.")
        .noMore();
  }

