  @Test
  public void detected() {
    SourceFile file = JavaAstScanner.scanSingleFile(new File("src/test/files/checks/SynchronizedClassUsageCheck.java"), new SynchronizedClassUsageCheck());
    checkMessagesVerifier.verify(file.getCheckMessages())
        .next().atLine(2).withMessage("Replace the synchronized class 'Vector' by an unsynchronized one.")
        .next().atLine(3)
        .next().atLine(4).withMessage("Replace the synchronized class 'Hashtable' by an unsynchronized one.")
        .next().atLine(5)
        .next().atLine(6);
  }

