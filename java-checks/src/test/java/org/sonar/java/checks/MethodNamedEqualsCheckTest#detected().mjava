  @Test
  public void detected() {
    SourceFile file = JavaAstScanner.scanSingleFile(new File("src/test/files/checks/MethodNamedEqualsCheck.java"), new VisitorsBridge(new MethodNamedEqualsCheck()));
    checkMessagesVerifier.verify(file.getCheckMessages())
        .next().atLine(2).withMessage("Either override Object.equals(Object), or totally rename the method to prevent any confusion.")
        .next().atLine(14)
        .next().atLine(17)
        .next().atLine(23)
        .next().atLine(26)
        .next().atLine(35);
  }

