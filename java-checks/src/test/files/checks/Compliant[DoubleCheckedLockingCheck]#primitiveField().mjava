  public void primitiveField() {
    if (primitiveField == null) {
      synchronized (this) { // Compliant
        if (primitiveField == null) {
          primitiveField = 42;
        }
      }
    }
  }

