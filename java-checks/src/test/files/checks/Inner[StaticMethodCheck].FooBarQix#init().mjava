    private void init() { // Compliant: foo cannot be static because it references a non-static field
      new Plop(){
        void plop1(){
          instanceVariable = 0;
        }
      };
    }

