  public boolean hasNext() {
    boolean b = false;
    b = next() != null; // Noncompliant
    b = this.next() != null; // Noncompliant
    b = other.next() != null;
    b = next("a") != null;
    b = otherMethod() != null;
    return true;
  }

