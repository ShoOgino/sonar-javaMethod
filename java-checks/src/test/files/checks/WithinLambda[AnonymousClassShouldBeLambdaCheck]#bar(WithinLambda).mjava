  private void bar(WithinLambda a) {
    a.doSomething(
      (Action<Void>) () -> {
        new Thread(
          new Runnable() { // FN: not handled
            @Override
            public void run() {
              // do somehting
            }
          });
        return null;
      });
  }

