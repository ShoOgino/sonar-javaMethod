  void testPrivateFinal() {
    if (C == A) {} // Compliant
    if (A == C) {} // Compliant
    if (C == D) {} // Compliant
    if (D == C) {} // Compliant
    if (this.C == new FF()) {} // Compliant
    if (new FF() == C) {} // Compliant
  }

