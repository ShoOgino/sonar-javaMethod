  void foo6() {
    if (true) {  // compliant, not using equals.
      dispenseRed();
    } else if ("blue".equals(choice)) {
      dispenseBlue();
    } else if ("yellow".equals(choice2)) {
      dispenseYellow();
    } else {
      promptUser();
    }
  }

