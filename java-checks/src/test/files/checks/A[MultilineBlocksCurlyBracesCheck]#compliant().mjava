  void compliant() {
    if (condition)
      action();
    outerAction();

    if (condition) {
      firstActionInBlock();
      secondAction();
    } else if (condition) {
    } else {
      other();
    }
    thirdAction();

    String str = null;
    for (int i = 0; i < array.length; i++) {
      str = array[i];
      doTheThing(str);
    }
  }

