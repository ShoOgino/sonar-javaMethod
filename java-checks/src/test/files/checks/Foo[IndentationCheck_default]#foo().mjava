  void foo() {
    IntStream
      .range(1, 5)
      .map(a -> {
        return a + 1; // should be a valid position
      });
    IntStream
      .range(1, 5)
      .map(a -> {
        if (a == 5) {
          a--;
        }
         a += 1; // Noncompliant {{Make this line start at column 9.}}
        return a + 1;
      });
  }

