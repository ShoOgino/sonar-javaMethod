  public void assign(boolean parameter) {
    parameter = false;
    if (parameter) { // Compliant, false negative
      if (parameter) { // Compliant, false negative
      }
    }
    if (!parameter) { // Compliant, false negative
      if (!parameter) { // Compliant, false negative
      }
    }
  }

