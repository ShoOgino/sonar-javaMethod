    void foo() {
        IntStream
            .range(1, 5)
            .map((a -> {
                return a + 1;
            }));
        IntStream
            .range(1, 5)
            .map((a -> {
              return a + 1; // Noncompliant {{Make this line start at column 17.}}
            }));

        IntStream
            .range(1, 5)
            .map((
                a -> {
                    return a + 1;
                }));
    }

