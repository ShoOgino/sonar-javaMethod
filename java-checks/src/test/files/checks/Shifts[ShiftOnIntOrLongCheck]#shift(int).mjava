  public int shift(int a) {
    int b;
    b = a <<  31; // Compliant
    b = a >> -31; // Compliant
    b = a <<  32; // Nomcompliant
    b = a >> -32; // Noncompliant
    b = a <<  33; // Noncompliant
    b = a >> -33; // Noncompliant

    a <<=  31; // Compliant
    a >>= -31; // Compliant
    a <<=  32; // Noncompliant
    a >>= -32; // Noncompliant
    a <<=  33; // Noncompliant
    a >>= -33; // Noncompliant
    return b << +48; // Noncompliant
  }

