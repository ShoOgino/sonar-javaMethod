  public Helper classicCase() {
    if (helper == null) // Compliant because field is volatile
      synchronized (this) {
        if (helper == null)
          helper = new Helper();
      }
    return helper;
  }

