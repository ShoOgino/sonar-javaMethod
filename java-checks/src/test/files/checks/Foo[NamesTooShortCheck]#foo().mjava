  void foo() { // Compliant
    int a; // Compliant
    int index; // Compliant

    for (int i = 0; i < 42; i++) { // Compliant - exception
      int b; // Compliant
    }

    System.out.println(a + b); // Compliant
    fo(); // Compliant
  }

