  void foo() { // Compliant
    int a; // Non-Compliant
    int index; // Compliant

    for (int i = 0; i < 42; i++) { // Compliant - exception
      int b; // Non-Compliant
    }

    System.out.println(a + b); // Compliant
    fo(); // Compliant
  }

