  void qix3() {
    if (bar()) { // Noncompliant {{Replace this if-then-else statement by a single method invocation.}}
      bar(foo(), "foo", true);
    } else {
      bar(foo(), "foo", false);
    }
  }

