  public int hop(int a, boolean b) { // Noncompliant - b is used as selector in the inner class
    class InnerClass {
      int foo() {
        if (b) {
        } else {
        }
        return 0;
      }
    }
    return 0;
  }

