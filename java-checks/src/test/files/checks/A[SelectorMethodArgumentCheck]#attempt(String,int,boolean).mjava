  public void attempt(String name, int size, boolean isNice) { // Compliant
    boolean freeLiquor = isNice || (size > 165);
    if (freeLiquor) {
      offerLiquor(name);
    } else {
      tempt(name, isNice);
    }
  }

