  void equal_binary_expression() {
    if ("red" == choice) { // Noncompliant
      dispenseRed();
    } else if ("blue" == choice) {
      dispenseBlue();
    } else if ("yellow" == choice) {
      dispenseYellow();
    }
  }

