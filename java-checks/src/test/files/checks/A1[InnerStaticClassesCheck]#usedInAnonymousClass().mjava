  void usedInAnonymousClass() {
    final String myString = "";
    method(new toImplement() { // Compliant my String is used.
      void fun(String s) {
        myString.length();
      }
    });
  }

