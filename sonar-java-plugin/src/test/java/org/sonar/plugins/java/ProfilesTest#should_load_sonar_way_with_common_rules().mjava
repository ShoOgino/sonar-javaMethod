  @Test
  public void should_load_sonar_way_with_common_rules() {
    CommonRulesSonarWayProfile definition = new CommonRulesSonarWayProfile(new XMLProfileParser(universalRuleFinder()));
    RulesProfile profile = definition.createProfile(ValidationMessages.create());

    assertThat(profile.getName()).isEqualTo("Sonar way");
    assertThat(profile.getLanguage()).isEqualTo("java");
    assertThat(profile.getActiveRules()).hasSize(2);
  }

