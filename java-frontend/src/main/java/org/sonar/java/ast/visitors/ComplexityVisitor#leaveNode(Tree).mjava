  @Override
  public void leaveNode(Tree tree) {
    switch (tree.kind()) {
      case METHOD:
      case CONSTRUCTOR:
        leaveMethod((MethodTree) tree);
        break;
      default:
        // nothing to do
    }
  }

