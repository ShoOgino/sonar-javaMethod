  @Override
  public int hashCode() {
    if (hashCode == 0) {
      hashCode = next == null ? 0 : next.hashCode();
      hashCode = hashCode * 31 + element.hashCode();
    }
    return hashCode;
  }

