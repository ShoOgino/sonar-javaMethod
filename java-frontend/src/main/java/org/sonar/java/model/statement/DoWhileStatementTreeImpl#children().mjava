  @Override
  public Iterable<Tree> children() {
    return Arrays.asList(
      doKeyword,
      statement,
      whileKeyword,
      openParenToken,
      condition,
      closeParenToken,
      semicolonToken);
  }

