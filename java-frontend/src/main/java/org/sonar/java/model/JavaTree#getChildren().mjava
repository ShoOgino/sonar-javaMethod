  public List<Tree> getChildren() {
    if(children == null) {
      children = new ArrayList<>();
      for (Tree tree : children()) {
        children.add(tree);
      }
    }
    return children;
  }

