  @Override
  public final boolean is(Kind... kind) {
    Kind treeKind = kind();
    for (Kind kindIter : kind) {
      if (treeKind == kindIter) {
        return true;
      }
    }
    return false;
  }

