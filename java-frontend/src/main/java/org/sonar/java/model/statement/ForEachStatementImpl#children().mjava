  @Override
  public Iterable<Tree> children() {
    return Arrays.asList(
      forKeyword,
      openParenToken,
      variable,
      colonToken,
      expression,
      closeParenToken,
      statement);
  }

