    public String completeSignature(){
      String sign = "";
      if(owner != null) {
        sign += owner.getType().fullyQualifiedName();
      }
      sign += "#" + name + desc();
      return sign;
    }

