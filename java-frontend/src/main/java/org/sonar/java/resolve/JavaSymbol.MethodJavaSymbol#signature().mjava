    @Override
    public String signature() {
      if (signature == null) {
        signature = "";
        if (owner != null) {
          signature += owner.getType().fullyQualifiedName();
        }
        signature += "#" + name + desc();
      }
      return signature;
    }

