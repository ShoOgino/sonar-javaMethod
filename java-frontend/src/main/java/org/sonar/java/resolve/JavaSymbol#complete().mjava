  public void complete() {
    if (completer != null) {
      Completer c = completer;
      completer = null;
      completing = true;
      c.complete(this);
      completing = false;
      callbacks.forEach(Runnable::run);
    }
  }

