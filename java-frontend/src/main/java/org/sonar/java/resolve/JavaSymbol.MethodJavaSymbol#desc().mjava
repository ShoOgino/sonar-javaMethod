    private String desc() {
      if(desc == null) {
        desc = "("+getParametersTypes().stream().map(JavaType::fullyQualifiedName).collect(Collectors.joining(";"))+")";
      }
      return desc;
    }

