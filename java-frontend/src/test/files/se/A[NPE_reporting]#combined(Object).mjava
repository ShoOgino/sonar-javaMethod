  void combined(Object a) {
    Object b = new Object();
    if (a == null) { // flow@comb {{...}}
      b = a; // flow@comb {{...}}
      b.toString(); // Noncompliant [[flows=comb]] flow@comb {{b is dereferenced}}
    }
  }

