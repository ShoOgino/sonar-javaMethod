  public void nonTransitiveConditions(Object a, Object b, Object c) {
    if (a != b) {
      if (b != c) {
        if ( a != c) {
          log("Are same!");
        } else {
          log("Not same!");
        }
      }
    }
  }

