  public void unlockingInTryCatch() {
    Lock lock = new ReentrantLock();
    try {
      lock.lock(); // Noncompliant
    } finally {
      try {
        lock.unlock();
      } catch (Exception ex) {
        ex.printStackTrace();
      }
    }
  }

