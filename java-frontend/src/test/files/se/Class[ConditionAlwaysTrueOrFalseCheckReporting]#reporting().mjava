  void reporting() {
    boolean a = true;
    boolean b = true;
    if (unknown() && a // Noncompliant [[sc=22;ec=23]]
      && b) { // Noncompliant [[sc=10;ec=11]]
    }

    if (unknown() && a) { // Noncompliant [[sc=22;ec=23]]
    }
  }

