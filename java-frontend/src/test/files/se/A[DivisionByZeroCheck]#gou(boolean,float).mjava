  void gou(boolean b, float r) {
    float z1 = 0.0f;
    if (b) {
      z1 = 3.0f;
    } else {
      r = 1.0f;
    }
    r %= z1; // Noncompliant {{Make sure "z1" can't be zero before doing this modulation.}}
  }

