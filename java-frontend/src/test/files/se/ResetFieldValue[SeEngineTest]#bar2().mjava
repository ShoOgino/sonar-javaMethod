  void bar2() {
    if (b) {
      foo();
      if (b) { } // Noncompliant
    }
    if (b2) {
      foo();
      if (b2) { } // compliant : static call can update static field
    }

    if (o == null) {
      foo();
      o.toString(); // Noncompliant
    }
  }

