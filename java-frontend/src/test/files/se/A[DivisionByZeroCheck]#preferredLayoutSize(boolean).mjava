  void preferredLayoutSize(boolean useBaseline) {
    class Dim {
      int width;
      int height;
    }
    Dim dim = new Dim();
    int maxAscent = 0;
    int maxDescent = 0;
    int width = 14;

    dim.width += width;
    width = maxAscent + maxDescent;
  }

