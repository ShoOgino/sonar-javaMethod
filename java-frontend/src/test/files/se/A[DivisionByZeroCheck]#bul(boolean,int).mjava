  void bul(boolean b, int r) {
    int z1 = 14;
    if (b) {
      z1 = 0;
    } else {
      z1 = 52;
    }
    r /= z1; // Noncompliant {{Make sure 'z1' can't be zero before doing this division.}}
  }

