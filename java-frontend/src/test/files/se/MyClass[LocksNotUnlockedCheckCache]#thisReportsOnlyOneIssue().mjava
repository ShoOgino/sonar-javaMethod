  public void thisReportsOnlyOneIssue() {
    Lock lock = new ReentrantLock();
    if(foo) {
      lock.lock();// Noncompliant
      ifStmt();
    } else {
      lock.lock();// Noncompliant
      elseStmt();
    }
    end();
  }

