  void qix(boolean b, int r) {
    int z1 = 0;
    if (b) {
      z1 = 3;
    } else {
      r = 1;
    }
    r = 1 / z1; // Noncompliant {{Make sure 'z1' can't be zero before doing this division.}}
  }

