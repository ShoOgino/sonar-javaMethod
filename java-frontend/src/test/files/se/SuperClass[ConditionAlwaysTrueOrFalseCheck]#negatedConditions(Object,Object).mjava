  public void negatedConditions(Object a, Object b) {
    if (!(a == b)) {
      if ( a != b) {   // Noncompliant
        log("Not same!");
      } else {
        log("Are same!");
      }
    }
  }

