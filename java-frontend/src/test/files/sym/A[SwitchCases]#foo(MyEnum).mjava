  void foo(MyEnum v) {
    switch (v) {
      case A:
        bar();
        break;
      case B:
        bar();
        break;
      case UNKNOWN: // not part of enum, should not be resolved
        bar();
        break;
      default:
        break;
    }
  }

