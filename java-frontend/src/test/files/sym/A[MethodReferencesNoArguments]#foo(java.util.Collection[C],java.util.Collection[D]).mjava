  void foo(java.util.Collection<C> cs, java.util.Collection<D> ds) {
    cs.stream()
      .filter(B::isFalse)
      .filter(C::isTrue)
      .map(C::up);

    ds.stream()
      .filter(d::bool)
      .filter(this.d::bool)
      .filter(getD()::bool)
      .filter(D::bool)
      .filter(A.D::bool);
  }

