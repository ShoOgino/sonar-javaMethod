  public void foo(java.util.List<String> list) {
    list.stream()
      .filter(s -> (s.length() > 42))
      .map(s -> test1(s))
      .map(b -> test2(b));

    list.stream()
      .map(String::length)
      .filter(x -> test3(x) > 0);
  }

