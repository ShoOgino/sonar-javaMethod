  void foo(java.util.concurrent.TimeUnit tu) {
    switch (tu) {
      case DAYS:
        bar();
        break;
      case HOURS:
        bar();
        break;
      default:
        break;
    }
  }

