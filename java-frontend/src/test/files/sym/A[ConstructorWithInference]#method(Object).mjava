  A<String> method(Object o) {
    new A<String>();

    new A<>();
    new A<>().foo(o);
    if (new A<>().value != null) {}

    I<String> var = new A<>();
    I var2 = new A<>();

    bar(new A<>()); // compile with java 8, but not 7

    new A<>(o);
    new A<>("test");

    new A<>(aString);

    return new A<>();
  }

