  @Test
  public void for_statement() {
    String p =
      "class Foo {"
        + "  void foo() {"
        + "    for( ; ; ) {"
        + "    }"
        + "  }"
        + "}";
    assertLastStatementlastTokenValue(p, "}");

    p =
      "class Foo {"
        + "  void foo() {"
        + "    for( ; ; )"
        + "      doStuff();"
        + "  }"
        + "}";
    assertLastStatementlastTokenValue(p, ";");
  }

