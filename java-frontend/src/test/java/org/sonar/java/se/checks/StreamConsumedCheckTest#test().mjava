  @Test
  void test() {
    CheckVerifier.newVerifier()
      .onFile("src/test/files/se/StreamConsumedCheck.java")
      .withCheck(new StreamConsumedCheck())
      .withClassPath(SETestUtils.CLASS_PATH)
      .verifyIssues();
  }

