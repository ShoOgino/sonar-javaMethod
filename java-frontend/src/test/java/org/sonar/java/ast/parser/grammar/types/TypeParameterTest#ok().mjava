  @Test
  public void ok() {
    LexerlessGrammarBuilder b = JavaLexer.createGrammarBuilder();

    assertThat(b, JavaLexer.TYPE_PARAMETER)
      .matches("identifier")
      .matches("identifier extends Foo")
      .matches("identifier extends Foo & Bar");
  }

