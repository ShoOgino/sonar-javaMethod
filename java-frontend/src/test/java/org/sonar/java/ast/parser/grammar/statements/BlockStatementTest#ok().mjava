  @Test
  public void ok() {
    LexerlessGrammarBuilder b = JavaLexer.createGrammarBuilder();

    assertThat(b, JavaLexer.BLOCK_STATEMENT)
      .matches("int a;")
      .matches("public class A {}")
      .matches("private enum A {}")
      .matches(";")
      .matches("foo();");
  }

