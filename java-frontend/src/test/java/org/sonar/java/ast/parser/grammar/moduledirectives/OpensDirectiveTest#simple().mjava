  @Test
  public void simple() {
    assertThat(JavaLexer.OPENS_DIRECTIVE)
      .matches("opens greetings ;")
      .matches("opens com.greetings;")
      .matches("opens com.greetings to org.foo.bar;")
      .matches("opens com.greetings to org.foo.bar, foo.bar.qix;");
  }

