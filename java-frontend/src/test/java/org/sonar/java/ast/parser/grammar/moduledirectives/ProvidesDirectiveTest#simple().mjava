  @Test
  public void simple() {
    assertThat(JavaLexer.PROVIDES_DIRECTIVE)
      .notMatches("provides com.Greetings;")
      .matches("provides com.Greetings with org.foo.Greetings;")
      .matches("provides com.Greetings with org.foo.Greetings, foo.bar.Salutations;");
  }

