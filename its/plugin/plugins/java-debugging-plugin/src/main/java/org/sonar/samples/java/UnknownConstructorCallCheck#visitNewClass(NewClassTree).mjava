  @Override
  public void visitNewClass(NewClassTree tree) {
    if (tree.constructorSymbol().isUnknown()) {
      context.reportIssue(this, tree.identifier(), "Constructor not resolved");
    }
  }

