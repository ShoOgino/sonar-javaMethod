  public boolean doubleMutexCondition() {
    // A 2-field variant of Double Checked Locking.
    if (!initialized) {
      synchronized (this) {
        if (!initialized) {
          initialized = true;
          return true;
        }
      }
    }
    return false;
  }

