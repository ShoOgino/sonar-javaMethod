  @Test
  public void if_statement() {
    String p =
      "class Foo {"
        + "  void foo(boolean test) {"
        + "    if (test) { }"
        + "  }"
        + "}";
    assertLastStatementlastTokenValue(p, "}");

    p =
      "class Foo {"
        + "  void foo(boolean test) {"
        + "    if (test) { }"
        + "    else { }"
        + "  }"
        + "}";
    assertLastStatementlastTokenValue(p, "}");
  }

