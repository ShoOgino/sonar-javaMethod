  @Test
  public void primary_basic_type() {
    assertThat(typeOfExpression("int.class")).isSameAs(symbols.classType);
    assertThat(typeOfExpression("int[].class")).isSameAs(symbols.classType);
    assertThat(typeOfExpression("int[][].class")).isSameAs(symbols.classType);
  }

