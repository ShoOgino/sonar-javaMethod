  private Tree firstMember(String code) {
    CompilationUnitTree compilationUnitTree = (CompilationUnitTree) p.parse(code);
    ClassTree classTree = (ClassTree) compilationUnitTree.types().get(0);
    return classTree.members().get(0);
  }

