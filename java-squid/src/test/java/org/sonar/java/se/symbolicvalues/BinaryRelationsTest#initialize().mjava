  @BeforeClass
  public static void initialize() {
    for (int i = 0; i < NUMBER_OF_VALUES; i++) {
      values[i] = new SymbolicValue(i);
    }
    SVa = values[0];
    SVb = values[1];
    SVc = values[2];
  }

