  @Test
  public void ok() {
    g.switchBlockStatementGroup.mock();

    assertThat(p, parse(""));
    assertThat(p, parse("switchBlockStatementGroup"));
    assertThat(p, parse("switchBlockStatementGroup switchBlockStatementGroup"));
  }

