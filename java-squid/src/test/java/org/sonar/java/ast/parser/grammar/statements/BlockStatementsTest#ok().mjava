  @Test
  public void ok() {
    LexerlessGrammarBuilder b = JavaLexer.createGrammarBuilder();

    assertThat(b, JavaLexer.BLOCK_STATEMENTS)
      .matches("")
      .matches(";")
      .matches("int a; foo();");
  }

