  @Test
  public void test() {
    p.setRootRule(g.ge);
    assertThat(p).matches(">=");

    p.setRootRule(g.sr);
    assertThat(p).matches(">>");

    p.setRootRule(g.srequ);
    assertThat(p).matches(">>=");

    p.setRootRule(g.bsr);
    assertThat(p).matches(">>>");

    p.setRootRule(g.bsrequ);
    assertThat(p).matches(">>>=");
  }

