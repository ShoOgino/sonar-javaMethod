  @Test
  public void realLife() {
    assertThat(p, parse("b >> 4"));
    assertThat(p, parse("b >>= 4"));
    assertThat(p, parse("b >>> 4"));
    assertThat(p, parse("b >>>= 4"));
    // Java 7: diamond
    assertThat(p, parse("new HashMap<>()"));
  }

