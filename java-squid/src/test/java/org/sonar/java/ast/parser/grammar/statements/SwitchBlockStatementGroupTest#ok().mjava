  @Test
  public void ok() {
    g.rule(JavaGrammar.SWITCH_LABEL).mock();
    g.rule(JavaGrammar.BLOCK_STATEMENTS).mock();

    assertThat(g.rule(JavaGrammar.SWITCH_BLOCK_STATEMENT_GROUP))
        .matches("switchLabel blockStatements");
  }

