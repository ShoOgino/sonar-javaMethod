  @Test
  public void ok() {
    g.classBodyDeclaration.mock();

    assertThat(p, parse(";"));
    assertThat(p, parse("; classBodyDeclaration"));
    assertThat(p, parse("; classBodyDeclaration classBodyDeclaration"));
  }

