  @Test
  public void ok() {
    g.basicType.mock();
    g.classType.mock();

    assertThat(p, parse("basicType"));
    assertThat(p, parse("classType"));

    assertThat(p, parse("basicType []"));
    assertThat(p, parse("classType []"));

    assertThat(p, parse("basicType [] []"));
    assertThat(p, parse("classType [] []"));
  }

