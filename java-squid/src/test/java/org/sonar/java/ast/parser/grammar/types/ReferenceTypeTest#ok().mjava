  @Test
  public void ok() {
    g.basicType.mock();
    g.classType.mock();

    assertThat(g.referenceType)
        .matches("basicType")
        .matches("classType")
        .matches("basicType []")
        .matches("classType []")
        .matches("basicType [] []")
        .matches("classType [] []");
  }

