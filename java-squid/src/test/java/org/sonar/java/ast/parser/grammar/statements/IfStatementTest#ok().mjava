  @Test
  public void ok() {
    g.block.mock();
    g.parExpression.mock();

    assertThat(g.ifStatement)
        .matches("if parExpression block")
        .matches("if parExpression block else block");
  }

