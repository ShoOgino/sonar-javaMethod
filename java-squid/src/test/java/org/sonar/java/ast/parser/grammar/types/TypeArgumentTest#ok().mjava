  @Test
  public void ok() {
    g.referenceType.mock();

    assertThat(p)
        .matches("referenceType")
        .matches("?")
        .matches("? extends referenceType")
        .matches("? super referenceType");
  }

