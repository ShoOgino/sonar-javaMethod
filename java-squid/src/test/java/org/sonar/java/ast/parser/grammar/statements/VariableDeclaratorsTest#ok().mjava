  @Test
  public void ok() {
    g.variableDeclarator.mock();

    assertThat(p, parse("variableDeclarator , variableDeclarator, variableDeclarator"));
    assertThat(p, parse("variableDeclarator , variableDeclarator"));
    assertThat(p, parse("variableDeclarator"));
  }

