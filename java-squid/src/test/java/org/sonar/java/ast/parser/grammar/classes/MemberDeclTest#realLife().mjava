  @Test
  public void realLife() {
    assertThat(JavaLexer.MEMBER_DECL)
      .matches("Map<SomeType<?>,SomeType<?>> member;")
      .matches("Map < SomeType < ? > , SomeType < ? > > member;")
      .matches("int member()@Foo[]{}")
      .matches("int member() throws @Foo IllegalStateException {}");
  }

