  @Test
  public void ok() {
    g.basicType.mock();
    g.classType.mock();

    assertThat(p)
        .matches("basicType")
        .matches("classType")
        .matches("basicType []")
        .matches("classType []")
        .matches("basicType [] []")
        .matches("classType [] []");
  }

