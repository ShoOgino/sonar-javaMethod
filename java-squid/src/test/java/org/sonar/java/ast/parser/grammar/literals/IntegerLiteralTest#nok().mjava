  @Test
  public void nok() {
    assertThat(g.rule(JavaGrammar.INTEGER_LITERAL))
        .notMatches("0.1")
        .notMatches("_0")
        .notMatches("_d")
        .notMatches("._list");
    assertThat(g.rule(JavaGrammar.LONG_LITERAL))
        .notMatches("0.1")
        .notMatches("_0")
        .notMatches("_d")
        .notMatches("._list");
  }

