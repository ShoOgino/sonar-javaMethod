  @Test
  public void nok() {
    assertThat(g.integerLiteral)
        .notMatches("0.1")
        .notMatches("_0")
        .notMatches("_d")
        .notMatches("._list");
    assertThat(g.longLiteral)
        .notMatches("0.1")
        .notMatches("_0")
        .notMatches("_d")
        .notMatches("._list");
  }

