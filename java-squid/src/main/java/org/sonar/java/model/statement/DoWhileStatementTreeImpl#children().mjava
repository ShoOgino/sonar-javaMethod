  @Override
  public Iterable<Tree> children() {
    return Lists.newArrayList(
      doKeyword,
      statement,
      whileKeyword,
      openParenToken,
      condition,
      closeParenToken,
      semicolonToken);
  }

