  @Override
  public Iterator<Tree> childrenIterator() {
    return Iterators.concat(
      Iterators.forArray(
        enclosingExpression,
        identifier
      ),
      arguments.iterator(),
      Iterators.singletonIterator(classBody)
    );
  }

