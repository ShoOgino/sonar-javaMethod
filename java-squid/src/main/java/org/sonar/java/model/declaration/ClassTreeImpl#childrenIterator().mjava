  @Override
  public Iterator<Tree> childrenIterator() {
    return Iterators.concat(
      Iterators.forArray(
        modifiers,
        simpleName,
        superClass
        ),
      superInterfaces.iterator(),
      members.iterator()
      );
  }

