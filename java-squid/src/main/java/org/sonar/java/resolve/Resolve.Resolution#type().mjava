    public Type type() {
      if (type == null) {
        if(symbol.isKind(Symbol.MTH)) {
          return ((Type.MethodType)symbol.type).resultType;
        }
        return symbol.type;
      }
      return type;
    }

