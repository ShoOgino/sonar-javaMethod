    @Override
    public int hashCode() {
      if (hashCode == 0) {
        Hasher hasher = new Hasher();
        forEach(hasher);
        hashCode = hasher.result;
      }
      return hashCode;
    }

