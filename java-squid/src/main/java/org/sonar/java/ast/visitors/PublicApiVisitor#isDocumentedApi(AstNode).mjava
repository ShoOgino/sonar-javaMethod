  private boolean isDocumentedApi(AstNode astNode) {
    // TODO
    for (Trivia trivia : astNode.getToken().getTrivia()) {
      if (trivia.isComment()) {
        return true;
      }
    }
    return false;
  }

