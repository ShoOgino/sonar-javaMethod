  /**
   * 3.10. Literals
   */
  private void literals() {
    literal.is(or(
        FLOATING_LITERAL,
        INTEGER_LITERAL,
        CHARACTER_LITERAL,
        LITERAL,
        TRUE,
        FALSE,
        NULL));
  }

