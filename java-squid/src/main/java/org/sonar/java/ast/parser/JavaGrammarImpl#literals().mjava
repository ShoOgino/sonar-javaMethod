  /**
   * 3.10. Literals
   */
  private void literals() {
    literal.is(firstOf(
        FLOAT_LITERAL,
        INTEGER_LITERAL,
        DOUBLE_LITERAL,
        LONG_LITERAL,
        CHARACTER_LITERAL,
        LITERAL,
        TRUE,
        FALSE,
        NULL));
  }

