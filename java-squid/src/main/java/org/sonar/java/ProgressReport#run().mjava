  @Override
  public void run() {
    while (!Thread.interrupted()) {
      try {
        Thread.sleep(period);
      } catch (InterruptedException e) {
        return;
      }
      LOG.info(message);
    }
  }

