  @Override
  public void run() {
    while (!Thread.interrupted()) {
      try {
        Thread.sleep(period);
        logger.info(message);
      } catch (InterruptedException e) {
        thread.interrupt();
      }
    }
    logger.info(stopMessage);
  }

