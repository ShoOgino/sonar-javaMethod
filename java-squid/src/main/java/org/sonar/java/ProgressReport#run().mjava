  @Override
  public void run() {
    while (!Thread.interrupted()) {
      try {
        Thread.sleep(period);
        LOG.info(message);
      } catch (InterruptedException e) {
        thread.interrupt();
      }
    }
  }

